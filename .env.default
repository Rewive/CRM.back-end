#server
PORT= #Порт

#DB
MONGODB_URI= #Ссылка на БД

#JWT
JWT_SECRET= #Секретный токен JWT
JWT_EXPIRES_IN= #Время жизни JWT токена

#Fastify
MAX_PARAMS_LENGTH= #максимальное количество символов в динамических параметрах

#Throttle
TTL= #Интервал защиты
LIMIT= #максимальное количество запросов, отправленых на сервер с 1 IP в интервале TTL